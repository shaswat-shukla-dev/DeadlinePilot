# Copy to .env for local development
# Points the frontend to the deployed (or local) backend API

# Local dev: leave blank to use the CRA proxy (see package.json "proxy")
# Production: set to your App Engine backend URL, e.g.
# REACT_APP_API_URL=https://YOUR_PROJECT_ID.appspot.com
REACT_APP_API_URL=
